<p>Opens the locally served version of this post in a new browser tab.</p>
<p><strong>Requires the Eleventy development server to be running</strong> (usually via <code>npm start</code> in a separate terminal) on <code>http://localhost:8080</code>.</p>